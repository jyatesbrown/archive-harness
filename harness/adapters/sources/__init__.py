"""One module per approved Work Order 002 source (Amendment B roster).

Each class fixes the extraction configuration for its source; run-time
parameters (URL, client, timeout) come from `sources.json` via the runner.
Nothing here interprets record contents.
"""
